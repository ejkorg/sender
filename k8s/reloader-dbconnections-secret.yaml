apiVersion: v1
kind: Secret
metadata:
  name: reloader-dbconnections
  namespace: default
type: Opaque
data:
  # Base64-encoded JSON file contents. Use a secure mechanism to create this secret (e.g. kubectl create secret, sealed-secrets, or your cloud provider secrets).
  dbconnections.json: "REPLACE_WITH_BASE64_DBCONNECTIONS_JSON"

# Example: create the secret locally from a file (do not commit the file):
# kubectl create secret generic reloader-dbconnections --from-file=dbconnections.json=/path/to/dbconnections.json
